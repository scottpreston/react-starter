{
  "author": "scott preston",
  "name": "react-starter",
  "description": "simple project to highlight react with routing and redux.",
  "version": "0.0.1",
  "dependencies": {
    "browserify": "^13.0.1",
    "http-server": "^0.9.0",
    "jquery": "^3.0.0",
    "react": "^15.1.0",
    "react-dom": "^15.1.0",
    "react-redux": "^4.4.5",
    "react-router": "^2.4.1",
    "redux": "^3.5.2"
  },
  "scripts": {
    "build": "babel src -d lib && browserify lib/*.js -o dist/app.js",
    "prestart": "npm run build",
    "start": "http-server -p 5000"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0"
  }
}
